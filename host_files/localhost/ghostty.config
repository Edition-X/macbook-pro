# Managed by Ansible.
# Treat the left Option key as Alt so shell/editor Meta shortcuts work
# consistently while keeping the right Option key available for macOS
# special-character input.
macos-option-as-alt = left
